name: sub_domain
orm_name: SubDomain

cache_diffbase:
  enabled: true
  fields:
    - name: name
      orm_name: Name
      type: string

cache_tool:
  enabled: false

pubsub:
  message:
    enabled: true
    fields:
      - name: name
        type: string
