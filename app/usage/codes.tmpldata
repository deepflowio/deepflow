{
    "package": "usage",
    "codes": [
        "acl_gid, direction",
        "acl_gid, direction, ip, tap_type",
        "acl_gid, direction, ip_path, tap_type",
        "acl_gid, direction, protocol, ip, server_port, tap_type",
        "acl_gid, direction, protocol, ip_path, server_port, tap_type"
    ]
}
