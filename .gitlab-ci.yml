variables:
  GIT_SUBMODULE_STRATEGY: recursive

stages:
  - verify
  - package

verify-x64:
  stage: verify
  tags:
    - x64

  script:
    - cargo build
    - cargo test
    - cargo fmt --all -- --check

package-x64:
  stage: package
  tags:
    - x64

  script:
    - cargo build --release
  only:
    - master
    - /B_LC_RELEASE_.*/
    - /feature-.*/