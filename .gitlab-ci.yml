stages:
  - verify
  - package

verify-x64:
  stage: verify
  tags:
    - x64

  script:
    - cargo build
    - cargo test

package-x64:
  stage: package
  tags:
    - x64

  script:
    - cargo build --release
  only:
    - master
    - /B_LC_RELEASE_.*/
    - /feature-.*/